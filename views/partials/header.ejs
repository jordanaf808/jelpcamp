<!DOCTYPE html>
<html>
	<head>
		<title>Yelp Camp 2020</title>
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
		<link rel = "stylesheet" href="/stylesheets/main.css">
	</head>
	<body>
		
	<nav class="navbar navbar-default navbar-fixed-top">
		<div class="container-fluid">
			<div class="navbar-header">
				<%if(!currentUser){%>
					<a class="navbar-brand" href="/">YelpCamp</a>
					<% } else { %>
					<a class="navbar-brand" href="/campgrounds">YelpCamp</a>
					<% } %>	
			</div>
			<button class="nav-toggle" aria-label="toggle navigation">
				<span class="hamburger"></span>
			</button>
			<!-- <div class="collapse navbar-collapse"> -->
				<ul class="nav navbar-nav navbar-right nav__list">
					<%if(!currentUser){%>
						<li class="nav__item"><a href="/login" id="nav__link">Login</a></li>
						<li class="nav__item"><a href="/register" id="nav__link">Sign Up</a></li>
					<% } else { %>
					<li class="nav__item"><a href="#" id="nav__link"><%=currentUser.username%> is signed in!</a></li>
					<li class="nav__item"><a href="/logout" id="nav__link">Logout</a></li>
					<% } %>
				</ul>
			</div>
		</div>
	</nav>
<div class="container">
<!-- since our flash warning is always displayed our 'if' statement relies on whether
there is any content in the 'error' or not. if so display, if not, dont display. -->
	<% if(error && error.length > 0){ %>
	<div class="alert alert-danger" role="alert">
	<h4><%= error %></h4>
	</div>
	<% } %>
	<% if(success && success.length > 0){ %>
	<div class="alert alert-success" role="alert">
	<h4><%= success %></h4>
	</div>
	<% } %>
</div>


		
		
		